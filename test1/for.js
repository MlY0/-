for (
  // 반복문 돌기전에 반복문 안에서 쓸 변수들을 할당해놓는곳
  // 변수 초기화 = 변수에 값을 준다
  let i = 0;
  // 조건
  // 이 조건이 참일때만 반복합니다
  i < 10;
  // 반복문이 한바퀴 돌때마다 실행되는 줄
  // i++ // i = i + 1 // i += 1
  i++
) {
  // 내용
}

//이것도 가능
for (let i = 0; i <= 10; i++) {
  console.log('i의 값은' + i + '입니다')
}

